import os

# default config
class Config(object):
    REGION = "eu-west-1"
    # memcache local address
    MEMCACHE_ADDRESS = (
        "faiss-based-sentence-similarity-retrieval.y9tsob.cfg.euw1.cache.amazonaws.com"
    )
